# Postman Collection Dashboard

## Install

```sh
npm install
```

## Environment variables

Set `COLLECTIONS_DIR` in `server/.env`. Optionally set `RESULTS_FILE` to override `server/data/results.json`.

## Run

```sh
npm run dev
```
