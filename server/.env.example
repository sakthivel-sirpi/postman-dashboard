# Required. No default. Absolute directory containing the Postman collection and environment JSON files.
COLLECTIONS_DIR="/absolute/path/to/your postman collections folder"

# Optional. Defaults to server/data/results.json. File used to persist sanitized run state across restarts.
RESULTS_FILE="/absolute/path/to/results.json"
